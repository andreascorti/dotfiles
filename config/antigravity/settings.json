{
  "colorScheme": "light",
  "enableTelemetry": false,
  "permissions": {
    "allow": [
      "command(gh)",
      "command(curl)",
      "command(git)",
      "command(php artisan)",
      "command(npm)",
      "command(npx)",
      "command(pnpm)",
      "command(docker)",
      "command(docker-compose)",
      "command(make)",
      "command(brew)",
      "command(sed)",
      "command(cat)",
      "command(ls)",
      "command(grep)",
      "command(find)",
      "command(echo)",
      "command(mkdir)",
      "command(cp)",
      "command(mv)",
      "command(touch)",
      "command(chmod)",
      "command(pwd)",
      "command(which)",
      "command(whoami)",
      "command(date)",
      "command(wc)",
      "command(sort)",
      "command(uniq)",
      "command(head)",
      "command(tail)",
      "command(diff)",
      "command(awk)",
      "command(cut)",
      "command(tr)",
      "command(open)",
      "command(stackit)",
      "command(node)",
      "command(python3)",
      "command(code)",
      "command(lsof)",
      "command(timeout)",
      "command(xargs)",
      "mcp(playwright/*)",
      "mcp(figma-remote-mcp/*)",
      "mcp(code-to-figma/*)",
      "mcp(angular-cli/*)",
      "mcp(obsidian/*)",
      "read_url(*)"
    ],
    "deny": [
      "command(rm -rf)",
      "command(rm -fr)",
      "command(sudo)",
      "command(mkfs)",
      "command(dd)",
      "command(git push --force)",
      "command(git reset --hard)",
      "write_file(/Users/ascorti/.bashrc)",
      "write_file(/Users/ascorti/.zshrc)",
      "write_file(/Users/ascorti/.ssh)",
      "read_file(/Users/ascorti/.ssh)",
      "read_file(/Users/ascorti/.aws)",
      "read_file(/Users/ascorti/.config/gh)",
      "read_file(/Users/ascorti/.git-credentials)",
      "read_file(/Users/ascorti/.docker/config.json)",
      "read_file(/Users/ascorti/Library/Keychains)"
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "node /Users/ascorti/.claude/statusline.mjs",
    "enabled": true
  },
  "toolPermission": "always-proceed",
  "trustedWorkspaces": [
    "/Users/ascorti/workspaces/angular.quickstart",
    "/Users/ascorti/workspaces/agents-exp/design-system-migration"
  ]
}
